@page "/list/{PlatformString}"
@page "/list/{PlatformString}/{SearchText}"
@using AppProject.Shared;
@inherits BasePage

@foreach (Item item in items)
{
    <div class="item p-2 mt-3">
        @item.Name
    </div>
}